# Autenticação do /backoffice (obrigatório — sem isto o backoffice fica bloqueado)
# Escolha uma palavra-passe forte; é a única credencial que fica fora da base de dados,
# precisamente para poder proteger o acesso ao resto das definições.
BACKOFFICE_PASSWORD=

# Opcional: segredo usado para assinar o cookie de sessão. Se não for definido,
# é usado o valor de BACKOFFICE_PASSWORD. Recomendado defini-lo à parte em produção.
BACKOFFICE_SESSION_SECRET=

# Domínio próprio do site (sem barra no fim), ex: https://goodvibesespartalhouse.pt
# Usado onde é preciso um endereço absoluto: pré-visualização ao partilhar o link no
# WhatsApp/Facebook, sitemap.xml e robots.txt. Se não for definido, é usado o domínio
# de produção da Vercel — o site funciona na mesma, mas as partilhas apontam para lá.
NEXT_PUBLIC_SITE_URL=

# Segredo para os endereços chamados por um cron externo (sincronização iCal,
# mensagens automáticas, submissão diária ao SIBA). Sem isto, esses endereços só
# respondem a quem tiver sessão de backoffice iniciada — nunca ficam abertos ao
# público. No cron, junte-o ao pedido de uma destas formas:
#   cabeçalho:  Authorization: Bearer O_SEU_SEGREDO
#   ou no URL:  .../api/ical/import?key=O_SEU_SEGREDO
CRON_SECRET=

# Base de dados Postgres (obrigatório — ex: Supabase, Neon, ou qualquer Postgres).
# No Supabase: Project Settings → Database → Connection string → "URI".
# Em hosting sem servidor (Vercel, etc.) use a connection string do "connection
# pooler" (porta 6543 no Supabase), não a ligação direta — evita esgotar as
# ligações à base de dados quando há vários pedidos em simultâneo.
# Formato: postgres://utilizador:password@host:porta/nome_da_bd
DATABASE_URL=

# Fotos do site (capa + galeria), guardadas no Supabase Storage.
# 1. No mesmo projeto Supabase da DATABASE_URL: Storage → New bucket → nome "fotos" →
#    marcar "Public bucket" (sem isto as fotos não aparecem no site).
# 2. Project Settings → API → "Project URL" → SUPABASE_URL
# 3. Project Settings → API → "service_role" key (não a "anon") → SUPABASE_SERVICE_ROLE_KEY
#    (esta chave tem acesso total — nunca a exponha no código do browser, só aqui no servidor)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
